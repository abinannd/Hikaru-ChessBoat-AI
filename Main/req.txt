The PyInstaller spec appears to omit PyTorch completely.

Investigate the spec file.

Verify whether torch is being analyzed and bundled.

The current spec only includes hidden imports for the src package.

It does not include torch or collect its binaries.

Determine whether the correct solution is:

- collect_all("torch")
- collect_submodules("torch")
- collect_dynamic_libs("torch")
- or relying on the built-in PyInstaller torch hook.

Do not simply add random hidden imports.

Verify that:

import torch

works inside the packaged executable before continuing.

Also verify whether including ('src', 'src') as datas is necessary, since Python source files copied as data are not imported as modules.